'use strict';

/*var swaggerServer = require('swagger-server');
var debug = require('debug')('mobile-api-gateway:server');
var server = new swaggerServer.Server();
server.parse('mobile-monitoring-login-api.yaml');


server.listen(8000, function() {
    console.log('The Swagger mobile api gateway is now running at http://localhost:8000');
});*/

